// DEPRECATED — the Brainstorm section was folded into Research on 2026-07-30.
// Research is now the single discussion surface: project chooser on the left,
// the same capture conversation on the right (see ResearchView.tsx).
//
// Kept only so any stray import keeps compiling. Safe to delete this file.
export { default } from "./ResearchView";
