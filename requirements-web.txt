# AURA web bridge — extra deps for the React/Electron face.
# Install alongside requirements.txt:  pip install -r requirements-web.txt
fastapi
uvicorn[standard]
websockets

# Office round-trip editing for the Domain's Documentation tab
# (open a Word/Excel/PowerPoint file from OneDrive, edit it, write it back)
python-docx
openpyxl
python-pptx
